#include <iostream>
using namespace std;

int main()
{

    /*int temp[10];
    cout << sizeof(temp) << endl;

    int *ptr = &temp[0];
    cout<< sizeof(ptr) << endl;
    cout<< sizeof(*ptr) << endl;
    cout<< sizeof(&ptr) << endl;
    */

    /* int a[5] = {2,3,5};
     cout<< &a[0] <<endl;
     cout<< &a <<endl;
     cout<< a <<endl; // all of these cout pointing to 1st address


 int *p = &a[0];
 cout << p <<endl;
 cout << *p <<endl;cout << &p <<endl; // address of this is different coz it creates another memory block
 */

    /*
    // ##  symbol table

    int arr [10];
    // arr = arr +1 ; we can't do like this we can't reassign arr address

    // we can do like this

    int *ptr = &arr[0];
    cout << ptr << endl;
    ptr = ptr +1;
    cout <<ptr <<endl;*/
}